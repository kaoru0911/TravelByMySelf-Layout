//
//  ViewPointAndPathStoraged.swift
//  travelToMySelfLayOut
//
//  Created by 倪僑德 on 2017/4/19.
//  Copyright © 2017年 Chiao. All rights reserved.
//

import Foundation


class ViewPointAndPathStoraged: NSObject {
    //打包資料
    /*
    func unPackData(<#parameters#>) -> <#return type#> {
        <#function body#>
    }
    
    //確認出發地國家是否等同旅遊國家
    func checkDepartureCountry(departureCountry:String, viewPoint:String) -> Bool {
        //將viewPont丟入googleMapＡＰＩ, 並catch回傳的國家
        
        //先確認景點的國籍
        let country = checkViewPointCountry(viewPoint: viewPoint)
        
        guard let viewPointCountry = country else {
            ErrorCatch.cantNotFindCountryOnGooglePlace
            return false
        }
        
        //確認兩國家是否相等
        let checkResult = checkTwoCountryIsTheSameOrNot(firstCountry: departureCountry, secondCountry: viewPointCountry)
        //回傳結果
        return checkResult
    }
    
    //回傳景點的國家
    func checkViewPointCountry(viewPoint:String)->String!{
        //用google place確認景點的國籍
        
        //回傳該國籍
        
        return nil
    }
    
    //判別兩個國家是否相等
    func checkTwoCountryIsTheSameOrNot(firstCountry:String, secondCountry:String)->Bool{
        
        var result : Bool = false
        //判別是否相等
        
        //回傳結果
        return result 
    }
    
*/
}
